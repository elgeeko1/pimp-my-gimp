version: '3'

services:
  influxdb:
    image: influxdb:latest
    container_name: influxdb
    hostname: influxdb
    networks:
      - pimp-my-gimp
    ports:
      - "8086:8086"
    env_file:
      - influx.env
    volumes:
      - influxdb-storage:/var/lib/influxdb
    deploy:
      restart_policy:
        condition: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8086/ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  telegraf:
    image: telegraf:latest
    container_name: telegraf
    hostname: telegraf
    networks:
      - pimp-my-gimp
    ports:
      - "8186:8186"
    depends_on:
      influxdb:
        condition: service_healthy
    links:
      - influxdb
    env_file:
      - influx.env
    volumes:
      - ./telegraf.conf:/etc/telegraf/telegraf.conf:ro
    deploy:
      restart_policy:
        condition: always
    
  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    hostname: grafana
    networks:
      - pimp-my-gimp
    ports:
      - "3000:3000"
    depends_on:
      influxdb:
        condition: service_healthy
    links:
      - influxdb
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=strutyourscuff
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana-storage:/var/lib/grafana
      - ./grafana/provisioning/datasources:/etc/grafana/provisioning/datasources
      - ./grafana/provisioning/dashboards:/etc/grafana/provisioning/dashboards
      - ./grafana/dashboards:/var/lib/grafana/dashboards
      - ./grafana/dashboards/pimp-my-gimp.json:/usr/share/grafana/public/dashboards/home.json
    deploy:
      restart_policy:
        condition: always

  pimp-my-gimp:
    image: elgeeko/pimp-my-gimp:latest
    container_name: pimp-my-gimp
    hostname: pimp-my-gimp
    networks:
      - pimp-my-gimp
    ports:
      - "80:80"
    depends_on:
      - telegraf
    links:
      - telegraf
    privileged: true
    deploy:
      restart_policy:
        condition: always
    
networks:
  pimp-my-gimp:
    driver: bridge

volumes:
  influxdb-storage:
  grafana-storage:
